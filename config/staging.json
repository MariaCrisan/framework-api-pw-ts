{
  "baseUrl": "https://replace-with-staging-api.example.com",
  "apiVersion": "v1",
  "timeout": 20000,
  "verifySsl": true,
  "endpoints": { "health": "/health", "login": "/auth/login", "protectedResource": "/users/me" },
  "auth": { "tokenField": "accessToken", "tokenType": "Bearer", "enabled": true }
}
