{
  "name": "playwright-api-test-framework",
  "version": "1.0.0",
  "private": true,
  "description": "A composable TypeScript and Playwright API test framework skeleton.",
  "scripts": {
    "bdd:generate": "bddgen",
    "test": "bddgen && playwright test",
    "test:headed": "bddgen && playwright test --headed",
    "test:smoke": "bddgen && playwright test --grep @smoke",
    "test:auth": "bddgen && playwright test --grep @auth",
    "test:health": "bddgen && playwright test --grep @health",
    "test:report": "playwright show-report playwright-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.10.5",
    "playwright-bdd": "^9.2.0",
    "typescript": "^5.7.2"
  }
}
